# 🚀 БЫСТРЫЙ СТАРТ - Sketchfab Forecasts

## 🐳 Docker - Самый простой способ! (РЕКОМЕНДУЕТСЯ)

### Установка Docker

Если Docker еще не установлен:
- **Windows/Mac**: [Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Linux**: `curl -fsSL https://get.docker.com | sh`

### Запуск одной командой

**Интерактивный запуск:**
```powershell
.\docker-start.ps1
```

Выберите режим:
- **1** - Быстрый старт (только сервер, 30 сек)
- **2** - Полный пайплайн (все этапы, 10-15 мин)

**Или напрямую:**
```bash
docker-compose up -d web
```

**Готово!** Откройте http://localhost:8080

### Полный пайплайн в Docker

```bash
# Автоматически
.\docker-start.ps1  # Выберите режим 2

# Или вручную
docker-compose --profile tools run --rm scraper
docker-compose --profile tools run --rm preprocessor
docker-compose --profile tools run --rm eda
docker-compose --profile tools run --rm trainer
docker-compose up -d web
```

### Полезные Docker команды

```bash
# Посмотреть логи
docker-compose logs -f web

# Остановить
docker-compose down

# Пересобрать
docker-compose up -d --build web
```

📖 **Подробнее:** [DOCKER.md](DOCKER.md)

---

## 💻 Альтернатива: Локальный запуск (без Docker)

### Что можно сделать сейчас?

### 1️⃣ Открыть веб-интерфейс
Откройте браузер и перейдите на:
```
http://localhost:8080
```

Вы увидите форму для ввода параметров 3D-модели. Попробуйте изменить значения и получить прогноз!

### 2️⃣ Протестировать API через PowerShell

**Health Check:**
```powershell
Invoke-RestMethod -Uri http://localhost:8080/health
```

**Получить прогноз:**
```powershell
$body = @{
    category_count = 2
    tag_count = 10
    description_length = 300
    face_count = 15000
    vertex_count = 8000
    animation_count = 1
    is_downloadable = $true
    is_premium_author = $true
    author_followers = 500
} | ConvertTo-Json

Invoke-RestMethod -Uri http://localhost:8080/api/predict -Method Post -Body $body -ContentType "application/json"
```

**Получить статистику:**
```powershell
Invoke-RestMethod -Uri http://localhost:8080/api/stats
```

### 3️⃣ Протестировать с реальными данными

Чтобы собрать данные с Sketchfab и обучить модель:

**Откройте новый терминал PowerShell и выполните:**

```powershell
# 1. Сбор данных (займет 5-10 минут)
go run cmd/scraper/main.go -limit=100 -sort=likes

# 2. Предобработка
go run cmd/preprocessor/main.go

# 3. Анализ данных (создаст графики)
python scripts/eda.py

# 4. Обучение ML модели
python scripts/train_model.py
```

После обучения модели сервер будет использовать реальную ML модель вместо демо-версии.

## 📊 Примеры запросов

### Пример 1: Простая модель
```json
{
  "category_count": 1,
  "tag_count": 5,
  "description_length": 100,
  "face_count": 5000,
  "vertex_count": 2500,
  "animation_count": 0,
  "is_downloadable": false,
  "is_premium_author": false,
  "author_followers": 50
}
```
**Ожидаемый результат:** Низкая популярность (1.5-2.5)

### Пример 2: Премиум модель
```json
{
  "category_count": 3,
  "tag_count": 12,
  "description_length": 500,
  "face_count": 20000,
  "vertex_count": 10000,
  "animation_count": 3,
  "is_downloadable": true,
  "is_premium_author": true,
  "author_followers": 1000
}
```
**Ожидаемый результат:** Высокая популярность (4.5-6.0)

## 📁 Структура файлов проекта

```
Sketchfab-forecasts-service/
├── 📄 README.md              - Основная документация
├── 📄 USAGE.md               - Детальное руководство
├── 📄 CONCLUSIONS.md         - Выводы исследования
├── 📄 PROJECT_SUMMARY.md     - Краткое описание
├── 📄 QUICKSTART.md          - Этот файл!
│
├── cmd/                      - Исполняемые команды
│   ├── scraper/             - Сбор данных с API
│   ├── preprocessor/        - Обработка данных
│   └── server/              - ⚡ Веб-сервер (ЗАПУЩЕН)
│
├── internal/                 - Внутренние пакеты
│   ├── api/                 - API клиент
│   ├── models/              - Модели данных
│   ├── preprocessing/       - Обработка данных
│   └── ml/                  - ML предиктор
│
├── scripts/                  - Python скрипты
│   ├── eda.py               - Анализ данных
│   ├── train_model.py       - Обучение ML
│   └── predict.py           - Прогнозирование
├── data/                     - Данные и графики
└── models/                   - ML модели
```

## ❓ Частые вопросы

**Q: Docker или локальный запуск?**
A: 🐳 **Docker намного проще!** Одна команда вместо установки Go + Python + зависимостей.

**Q: Сколько времени занимает полный пайплайн?**
A: 
- Быстрый старт (только сервер): **30 секунд**
- Полный пайплайн (все этапы): **10-15 минут**

**Q: Можно без сбора данных?**
A: Да! Просто запустите сервер: `docker-compose up -d web` или `go run cmd/server/main.go`

**Q: Docker занимает много места?**
A: Образ ~500MB. Данные и модели в локальных папках.

**Q: Сервер не запускается**
A: Проверьте, что порт 8080 свободен. Можно изменить порт в `.env` файле.

**Q: Можно ли запустить без Python?**
A: Да! Веб-сервер работает на Go и использует mock-прогнозирование. Python нужен только для обучения ML модели.

## 🎯 Следующие шаги

1. ✅ **Изучите веб-интерфейс** - http://localhost:8080
2. 📊 **Попробуйте API** - используйте примеры выше
3. 📚 **Прочитайте документацию** - README.md и USAGE.md
4. 🔬 **Соберите данные** - go run cmd/scraper/main.go
5. 🤖 **Обучите модель** - python scripts/train_model.py

## 💡 Подсказки

- Открыть **DevTools** в браузере (F12) чтобы видеть API запросы
- Изменяйте параметры и наблюдайте как меняется прогноз
- Высокие значения `author_followers` и `tag_count` увеличивают популярность
- Флаги `is_downloadable` и `is_premium_author` тоже важны!

---

**🎉 Проект готов к использованию!**

Для защиты курсовой работы см. файл **CONCLUSIONS.md**
**С Docker:**
1. ✅ `docker-compose up -d web` - запустить сервер
2. 📊 `.\docker-start.ps1` → режим 2 - полный пайплайн
3. 📚 Прочитайте [DOCKER.md](DOCKER.md) для продвинутых возможностей

**Локально:**
1. ✅ **Изучите веб-интерфейс** - http://localhost:8080
2. 📊 **Попробуйте API** - используйте примеры выше
3. 📚 **Прочитайте документацию** - [README.md](README.md) и [USAGE.md](USAGE.md)
4. 🔬 **Соберите данные** - `go run cmd/scraper/main.go`
5. 🤖 **Обучите модель** - `python scripts/train_model.py`

## 💡 Подсказки

**Docker:**
- 🚀 Один раз собрать, потом мгновенный запуск
- 📦 Все зависимости внутри - не нужно устанавливать Go/Python
- 🔄 Легко пересобрать: `docker-compose up -d --build web`
- 📊 Данные сохраняются в локальных папках `data/` и `models/`

**Веб-интерфейс:**
- Откройте **DevTools** в браузере (F12) чтобы видеть API запросы
- Изменяйте параметры и наблюдайте как меняется прогноз
- Высокие значения `author_followers` и `tag_count` увеличивают популярность
- Флаги `is_downloadable` и `is_premium_author` тоже важны!

---

**🎉 Проект готов к использованию!**

🐳 **Docker**: `docker-compose up -d web`  
💻 **Локально**: `go run cmd/server/main.go`

Откройте http://localhost:8080 и начинайте работу!

📚 Для защиты курсовой работы см. [CONCLUSIONS.md](CONCLUSIONS.md)