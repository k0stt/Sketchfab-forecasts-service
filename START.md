# 🚀 Система прогнозирования популярности Sketchfab

Анализирует теги, описание, полигоны и другие параметры для прогнозирования популярности и оценки качества 3D-моделей.

## Быстрый старт (3 шага)

### 1️⃣ Соберите данные
```powershell
go run cmd/scraper/main.go -sort="-likeCount" -date="30" -limit=500
```

### 2️⃣ Обучите модель
```powershell
go run cmd/preprocessor/main.go
python scripts/train_model_advanced.py
```

**С ограничением данных:**
```powershell
python scripts/train_model_advanced.py 100  # только 100 моделей
python scripts/train_model_advanced.py 250  # только 250 моделей
```

### 3️⃣ Запустите Web интерфейс
```powershell
go run cmd/server/main.go
```

**Откройте:** http://localhost:8080

## 🎯 Возможности

✅ Анализ текста (теги и описание)  
✅ Оценка качества модели (0-100%)  
✅ Учет оптимальных диапазонов полигонов  
✅ Рекомендации по улучшению  
✅ Все метрики доступны через Web  

## 📊 Web интерфейс

- **Прогнозирование** - введите параметры модели
- **Обучение** - выберите количество данных для обучения
- **Метрики** - просмотр точности модели
- **Статистика** - анализ собранных данных

## 🔧 Зависимости

```powershell
pip install scikit-learn pandas numpy joblib
go mod download
```

## 📈 Анализируемые параметры

**Численные:**
- Количество полигонов (с оптимальными диапазонами)
- Количество тегов
- Длина описания
- Подписчики автора
- и др.

**Текстовые (TF-IDF):**
- Теги модели
- Описание
- Категории

**Оценка качества (0-100%):**
1. Описание (25%)
2. Теги (25%)
3. Полигоны (20%)
4. Аккаунт (15%)
5. Технические характеристики (15%)

## 🌐 API

```bash
POST /api/predict
GET /api/model-info
GET /api/train
GET /api/stats
```

Подробнее в Web интерфейсе.

## 📁 Структура

```
cmd/scraper/      - Сбор данных
cmd/preprocessor/ - Предобработка
cmd/server/       - Web сервер
scripts/          - Python скрипты ML
data/             - Данные
models/           - Обученная модель
```

---

**Все управление через Web: http://localhost:8080** 🌐
